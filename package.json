{
  "name": "secret_manager",
  "version": "1.0.0-rc1",
  "private": true,
  "description": "Enterprise Secret Management & AI Agent Security Fortress",
  "workspaces": [
    "apps/*",
    "sdks/*"
  ],
  "scripts": {
    "web:dev": "npm --prefix apps/web run dev",
    "web:build": "npm --prefix apps/web run build",
    "web:test": "npm --prefix apps/web run test",
    "test": "make test",
    "test:all": "make test-all",
    "lint": "npm --prefix apps/web run lint"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
